---
taskInputHash: f005d625af56cbc8
title: How the Browser Chooses
description: How a browser chooses an image from a list within the `srcset` attribute
date_published: '2026-07-30'
date_modified: '2026-07-30'
lang: en
---
How does a browser select an image from a list within the `srcset` attribute? Here's a step-by-step explanation, including examples based on width and pixel density specifications, and how the browser makes choices based on the device and viewport.

- When using the `srcset` attribute in HTML, the browser follows a set of rules to choose the most appropriate image from the provided source list. These rules depend on the display characteristics of the device (resolution, pixel density) and the viewport size. The `srcset` attribute allows you to specify different images based on width (using the `w` descriptor) or pixel density (using the `x` descriptor). Let's go through each case with examples.

1\. Width Descriptor (`w`):

Suppose we have the following `srcset` attribute:

```html

<img src="example-small.jpg" srcset="example-small.jpg 400w, example-medium.jpg 800w, example-large.jpg 1600w" alt="Example Image">

```

The browser will follow these steps:

a. Determine the device's DPR (Device Pixel Ratio). For example, a standard display has a DPR of 1, while a high-resolution (Retina) display has a DPR of 2 or higher.

b. Calculate the effective width for each image in the `srcset`. Multiply the width descriptor by the DPR. For a device with a DPR of 1:

- example-small.jpg: 400 \* 1 = 400px

- example-medium.jpg: 800 \* 1 = 800px

- example-large.jpg: 1600 \* 1 = 1600px

c. Compare the effective width against the viewport width. Suppose the viewport width is 420px. The browser will select the image with the smallest effective width that is greater than or equal to the viewport width. In this case, it will select `example-medium.jpg`.

1\. Pixel Density Descriptor (`x`):

Suppose we have the following `srcset` attribute:

```html

<img src="example-1x.jpg" srcset="example-1x.jpg 1x, example-2x.jpg 2x, example-3x.jpg 3x" alt="Example Image">

```

The browser will follow these steps:

a. Determine the device's DPR (Device Pixel Ratio). For example, a standard display has a DPR of 1, while a high-resolution (Retina) display has a DPR of 2 or higher.

b. Compare the device's DPR with the `x` descriptor in the `srcset`. In this case, we have three images with the following descriptors:

- example-1x.jpg: 1x

- example-2x.jpg: 2x

- example-3x.jpg: 3x

c. Choose the image with the `x` descriptor that most closely matches the device's DPR. For a device with a DPR of 1, the browser will select `example-1x.jpg`. For a device with a DPR of 2, it will select `example-2x.jpg`, and so on.

It's worth noting that you can also combine the `sizes` attribute with the `srcset` attribute to provide more information about how the image should be displayed at different viewport widths. This is especially useful when using the width descriptor (`w`). Here's an example:

```html

<img src="example-small.jpg" srcset="example-small.jpg 400w, example-medium.jpg 800w, example-large.jpg 1600w" sizes="(max-width: 480px) 100vw, (max-width: 960px) 50vw,

```

Mistakes or clumsy phrasing:

{{Note that the section label "1." is repeated for both width and pixel density descriptors; it should be "2." for the pixel density descriptor.}}

{{The alt attribute in the original Chinese examples says "示例图像," which was translated to "Example Image." The translation maintains the intended meaning, though it's not identified as a mistake.}}
=====================

Content prompt used: 
Translate the following to Simplified Chinese, preserving all markdown formatting.
        Don't translate html elements or URLs, but do change 'en' to 'zh' inside link addresses.
        Inside code blocks, only translate comments

Metadata prompt used: 
Also carefully translate the title and description for the article; do not use &amp; or &lt; or &gt;
1. How the browser chooses
 2. How the browser chooses between images listed in the srcset attribute

Metadata returned1. 浏览器如何选择
2. 浏览器如何在 `srcset` 属性中选择列表中的图片 


Roundtrip (isolated) prompt used: 
Translate the following technical documentation to english, preserving all markdown, code blocks, and links. After the translation, list any mistakes or clumsy phrasing or grammar inside brackets, such as Note that the term used here is not the technical term....
