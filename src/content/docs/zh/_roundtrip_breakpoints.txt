---
title: Breakpoints and Images
description: Why some (but not all) breakpoints matter for your images
taskInputHash: 32b3202b064608d3
lang: en
date_published: '2023-04-03'
date_modified: '2023-04-03'
date_updated: '2023-04-03'
---

### Background

Small screens and large screens require different layouts. When using `srcset` and `sizes`, we need to know when the layout changes.

Web developers must decide what content to shrink, hide, or reposition on smaller screens—or more commonly, what content to expand, reveal, or add on larger screens. They have only limited information to work with. Is the user on a tablet, a phone in landscape mode, or a small browser window?

We need to choose an arbitrary viewport width when the layout changes. This is called a **breakpoint**. It's any number of CSS pixels ([not device pixels](/en/pixels-not-pixels)). Polypane has a [great article](https://polypane.app/blog/the-breakpoints-we-tested-in-2021-and-the-ones-to-test-in-2022/#the-breakpoints-to-develop-on-in-2023) on common breakpoints.

Some images (like logos, icons, or buttons) may be immune to layout changes caused by these breakpoints (applicable to [srcset density descriptors](/en/density-descriptors)).

Main content images will be limited by the size of their container. Typically, the main content area of a page will be limited to a certain width on larger screens, using `max-width`, while on smaller screens it will fill the entire viewport.

If there are multiple columns at some breakpoints, the calculation of effective size rules will become more difficult, as the percentage of viewport width occupied by the image will change.

### A Simple Approach

That said, don't overthink it. You'll likely be perfectly satisfied with the following approximations:

1. At what viewport width does the main column (or image container) stop growing? In that range of viewport widths, we can use `100vw` for the image `sizes` attribute to indicate that the image occupies the entire viewport width.
2. What is the maximum width the container reaches? We can set it to a fixed `width` for all other content.

If your answers are 1) 700px and 2) 800px, you can use the following `sizes` attribute:

```html
<!-- CSS pixels are used here, not device pixels or image pixels.-->
<img [...] sizes="(max-width: 700px) 100vw, 800px"  />
```

> You might think that the browser could handle all of these calculations for us nicely based on the CSS. Unfortunately, browsers are very eager to select the image URL well before they download the stylesheet. So, we have to do these calculations ourselves, and if we don't do them perfectly, we shouldn't blame the browsers.
=====================

Content prompt used: 
Translate the following to Simplified Chinese, preserving all markdown formatting.
        Don't translate html elements or URLs, but do change 'en' to 'zh' inside link addresses.
        Inside code blocks, only translate comments

Metadata prompt used: 
Also carefully translate the title and description for the article; do not use &amp; or &lt; or &gt;
1. Breakpoints & images
 2. Why some (but not all) breakpoints matter for your images

Metadata returned1. 断点和图像
2. 为什么某些（但不是所有）断点对您的图像很重要 


Roundtrip (isolated) prompt used: 
Translate the following technical documentation to english, preserving all markdown, code blocks, and links. After the translation, list any mistakes or clumsy phrasing or grammar inside brackets, such as Note that the term used here is not the technical term....
