---
taskInputHash: 9c82f13cad7a56f3
title: px ≠ pixels
description: Unit px ≠ browser px ≠ image pixels
sidebar_sort_priority: 900
date_published: '2026-07-30'
date_modified: '2026-07-30'
lang: en
---
### Unit Pixels ≠ Browser px ≠ Image Pixels

Thanks to Apple, CSS, and HTML pixels (`px`) **are almost always mapped to multiple device/printer pixels**. My phone's devicePixelRatio is 3.875; [what's yours?](https://www.mydevice.io/) 

My desktop's device pixel ratio changes with browser zoom, but the default setting is 1.5 (after all, my `System > Display > Scaling` is set to 150%).

Image files (excluding SVG) decode to a grid of pixels. **It is annoyingly difficult to get image pixels to display perfectly in a 1:1 ratio with device pixels - but with `srcset` you can get *close enough***, and some JavaScript can adjust the HTML element size after loading if you have truly justified reasons for needing the perfect mapping, even though such forbidden arts will not be revealed here.

> Other absolute [units (in, pc, pt, mm, cm, Q)](https://developer.mozilla.org/en/docs/Web/CSS/length) are defined in terms of a number of CSS pixels and pretend that everything is 96dpi. Developers avoid absolute units because it is generally better to use units derived from the text size or viewport/container dimensions. Mismatched units can ruin a layout if the user adjusts their `Accessibility > Text Size` multiplier, zooms the browser, or rotates their device. Needless to say, you should do all these things before launching your site.

[Mistakes or clumsy phrasing or grammar:]
- {{The original term "decodes" might sound slightly odd concerning image files. "Render" might be a more common term.}}
- {{The phrase "forbidden arts" might sound more like a colorful narrative than technical documentation, but it adds some stylistic flair.}}
- {{"Truly justified reasons" could be phrased simply as "valid reasons" to maintain concise and clear technical language.}}
=====================

Content prompt used: 
Translate the following to Swedish, preserving all markdown formatting.
        Don't translate html elements or URLs, but do change 'en' to 'sv' inside link addresses.
        Inside code blocks, only translate comments

Metadata prompt used: 
Also carefully translate the title and description for the article; do not use &amp; or &lt; or &gt;
1. px ≠ pixels
 2. Device px ≠ browser px ≠ image px

Metadata returned1. px ≠ pixlar
2. Enhetspx ≠ webbläsarpx ≠ bildpixlar 


Roundtrip (isolated) prompt used: 
Translate the following technical documentation to english, preserving all markdown, code blocks, and links. After the translation, list any mistakes or clumsy phrasing or grammar inside brackets, such as Note that the term used here is not the technical term....
