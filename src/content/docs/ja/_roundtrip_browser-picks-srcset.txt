---
taskInputHash: 190f900bcb01824b
title: How Browsers Select Images
description: How a browser selects images listed in the `srcset` attribute
date_published: '2026-07-30'
date_modified: '2026-07-30'
lang: en
---
How does a browser choose from the images listed in the `srcset` attribute? Here is a step-by-step explanation. We will introduce examples of width and density specifications, and how browsers make selections based on the device and viewport.

- When using the HTML `srcset` attribute, browsers employ a series of rules to choose the most appropriate image from the provided list of sources. These rules depend on both the display characteristics of the device (resolution, pixel density) and the viewport size. By using the `srcset` attribute, different images can be specified based on width (using the `w` descriptor) or pixel density (using the `x` descriptor). We will explain each case with examples.

1\. Width descriptor (`w`):

Consider the following `srcset` attribute:

```html

<img src="example-small.jpg" srcset="example-small.jpg 400w, example-medium.jpg 800w, example-large.jpg 1600w" alt="Example Image">

```

The browser follows these steps:

a. Determine the device's DPR (Device Pixel Ratio). For example, the DPR of a standard display is 1, but high-resolution (Retina) displays have a DPR of 2 or more.

b. Calculate the effective width of each image in the `srcset`. Multiply the width descriptor by the DPR. For devices with a DPR of 1:

- example-small.jpg: 400 \* 1 = 400px

- example-medium.jpg: 800 \* 1 = 800px

- example-large.jpg: 1600 \* 1 = 1600px

c. Compare the viewport width to the effective widths. Assume the viewport width is 420px. The browser chooses the smallest image with an effective width that is greater than or equal to the viewport width. In this case, `example-medium.jpg` is selected.

2\. Pixel density descriptor (`x`):

Consider the following `srcset` attribute:

```html

<img src="example-1x.jpg" srcset="example-1x.jpg 1x, example-2x.jpg 2x, example-3x.jpg 3x" alt="Example Image">

```

The browser follows these steps:

a. Determine the device's DPR (Device Pixel Ratio). For example, the DPR of a standard display is 1, but high-resolution (Retina) displays have a DPR of 2 or more.

b. Compare the `x` descriptors in the `srcset` with the device's DPR. In this case, there are three images with the following descriptors:

- example-1x.jpg: 1x

- example-2x.jpg: 2x

- example-3x.jpg: 3x

c. Choose the image with the `x` descriptor closest to the device's DPR. For a device with a DPR of 1, the browser selects `example-1x.jpg`. If the DPR is 2, it selects `example-2x.jpg`, and so on.

When using the width descriptor (`w`), combining the `srcset` attribute with the `sizes` attribute offers additional information on how images should be displayed at different viewport widths. This is particularly useful, as shown below:

```html

<img src="example-small.jpg" srcset="example-small.jpg 400w, example-medium.jpg 800w, example-large.jpg 1600w" sizes="(max-width: 480px) 100vw, (max-width: 960px) 50vw, 

```

{{No mistakes or clumsy phrasing noted.}}
=====================

Content prompt used: 
Translate the following to Japanese, preserving all markdown formatting.
        Don't translate html elements or URLs, but do change 'en' to 'ja' inside link addresses.
        Inside code blocks, only translate comments

Metadata prompt used: 
Also carefully translate the title and description for the article; do not use &amp; or &lt; or &gt;
1. How the browser chooses
 2. How the browser chooses between images listed in the srcset attribute

Metadata returned1. ブラウザが選択する方法  
2. ブラウザが`srcset`属性にリストされた画像から選択する方法 


Roundtrip (isolated) prompt used: 
Translate the following technical documentation to english, preserving all markdown, code blocks, and links. After the translation, list any mistakes or clumsy phrasing or grammar inside brackets, such as Note that the term used here is not the technical term....
